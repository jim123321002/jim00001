import assert from 'node:assert/strict';import { WORDS } from './words.js';assert.equal(WORDS.length,5);assert.deepEqual(WORDS.map(w=>w.word),['apple','book','cat','dog','sun']);assert.ok(WORDS.every(w=>w.meaning&&w.example&&w.emoji));console.log('word-game tests passed: 5 starter words');