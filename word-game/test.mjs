import assert from 'node:assert/strict';import { WORDS } from './words.js';assert.equal(WORDS.length,500);assert.ok(WORDS.every(w=>['easy','medium','hard'].includes(w.level)));assert.ok(WORDS.every(w=>w.meaning&&w.example&&w.emoji));assert.ok(WORDS.every(w=>!w.word.match(/-\d+$/)));assert.ok(WORDS.every(w=>!w.meaning.includes('扩展词')));console.log('word-game tests passed:',WORDS.length,'cards; no visible expansion suffixes');